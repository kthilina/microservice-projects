server:
  port: 8191
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/kthilina/microservice-projects-config-store
          search-paths:
            - 'service-config/*service'
          #username:
          #passphrase:



